/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.pexels.com',
      'images.unsplash.com',
      'res.cloudinary.com',
      'utfs.io',
    ],
  },
};

module.exports = nextConfig;
